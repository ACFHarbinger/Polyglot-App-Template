apiVersion: apps/v1
kind: Deployment
metadata:
  name: polyglot-app-template
  labels:
    app: polyglot-app-template
spec:
  replicas: 1
  selector:
    matchLabels:
      app: polyglot-app-template
  template:
    metadata:
      labels:
        app: polyglot-app-template
    spec:
      containers:
        - name: app
          # TODO: point at your real registry once infra/global/docker/Dockerfile is published there.
          image: polyglot-app-template:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: polyglot-app-template-config
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
