apiVersion: v1
kind: Service
metadata:
  name: {{ include "polyglot-app-template.name" . }}
  labels:
    {{- include "polyglot-app-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "polyglot-app-template.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
