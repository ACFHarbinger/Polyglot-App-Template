apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-repo-template.name" . }}
  labels:
    {{- include "dev-repo-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "dev-repo-template.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
