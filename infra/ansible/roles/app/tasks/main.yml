---
- name: Ensure app directory exists
  ansible.builtin.file:
    path: /opt/dev-repo-template
    state: directory
    mode: "0755"

- name: Template environment file
  ansible.builtin.template:
    src: env.j2
    dest: /opt/dev-repo-template/.env
    mode: "0600"
