# Project-wide Gradle settings.
# IDE (e.g. IntelliJ IDEA, Android Studio) settings configured through the IDE
# *will override* any settings specified in this file.
# For more details, see https://docs.gradle.org/current/userguide/build_environment.html

# JVM arguments for the Gradle daemon process.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Enable the configuration cache and parallel builds for faster incremental builds.
org.gradle.parallel=true
org.gradle.caching=true

# Kotlin code style for this project: "official" or "obsolete".
kotlin.code.style=official
