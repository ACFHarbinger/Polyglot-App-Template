[project]
name = "polyglot-pixi-env"
version = "0.1.0"
description = "Environment specification using Pixi for conda/pip packages."
authors = ["ACFHarbinger"]
channels = ["conda-forge"]
platforms = ["linux-64", "osx-64", "win-64"]

[dependencies]
python = ">=3.11"
numpy = ">=1.24"
pandas = ">=2.0"

[tasks]
test = "pytest"
